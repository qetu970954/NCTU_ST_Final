language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"

install:
  - "python setup.py install"
  - "pip install pytest-cov pytest"
  - "pip list"

script:
  # Execute tests under the 'tests' directory.
  - py.test --cov=./ tests/
  - codecov

after_success:
  - codecov
